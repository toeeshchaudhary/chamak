"""Chamak demo mode.

Self-contained data + scripts so the app boots into a meaningful state
without any network access. Everything in this subpackage is for showcases,
screen recordings, and first-time users — production logic must not import
from `demo` outside of the seeder."""
